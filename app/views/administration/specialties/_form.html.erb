  <%= form_with(model: [ :administration, specialty ], local: true) do |f| %>
  <% if specialty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(specialty.errors.count, "error") %> prohibited this specialty from being saved:</h2>
      <ul>
      <% specialty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<br>
<div class="field">
  <%= f.label :Nombre_de_la_especialidad %>
  <%= f.text_field :name, class: 'form-control',:maxlength => 100 %>
</div>

<br><br><center>
  
  <div class="actions">
  <%= f.submit "Aceptar", class: "btn btn-primary" %>

  </center></div>
<% end %>
</center>

